name: Arch build

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v1
    - run: docker build -t arch .
    - run: docker run -v $(pwd):/home/arch/mount arch cd mount '&&' make arch '&&' which map-io